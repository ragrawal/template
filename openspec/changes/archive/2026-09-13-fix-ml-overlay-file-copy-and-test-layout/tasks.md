## 1. Fail loudly when `package_name` can't be derived

- [x] 1.1 Detect an empty derived `package_name` in `templates/ml/copier.yml` as a post-render `_tasks` check (a pure-Jinja `validator:` cannot fire here: it only runs when the `package_name` question is actually asked, but `when: false` - required to keep the documented flagless, non-interactive invocation working - always skips asking it). The check runs `pkg="$(basename "$PWD")"` (which resolves correctly from the shell regardless of how `.`/relative paths were passed on the command line) and treats a missing `src/$pkg/estimator.py` as proof that `package_name` resolved empty during rendering, then deletes every path this template rendered and exits non-zero. Verify by running `copier copy templates/ml <dest> --trust` with destination `.` inside a directory generated by `templates/python-project`: the run exits non-zero with a message naming the missing file and telling the user to pass an explicit destination or `--data package_name=<name>`, and no file under `src/` remains afterward
- [x] 1.2 Confirm the existing happy path still works unchanged: `copier copy templates/ml <dest>/<package_name> --trust` still derives `package_name` from the destination folder name with no prompt and no error

## 2. Fail-and-cleanup for the missing-prerequisite check

- [x] 2.1 Rework `templates/ml/copier.yml` `_tasks` entry 1 into a single shell command that, when `.answers/.python_project.yml` is missing, removes every path this template renders (`src/<package_name>/estimator.py`, `src/<package_name>/schema.py`, `src/<package_name>/tasks/`, `webapp/`, `docker/`, `docs/ML_README.md`, `.dockerignore`, `.answers/.ml.yml`, and the test paths from Task 3) before exiting non-zero, and confirm it still runs before the `uv add scikit-learn ...` and `uv add --dev httpx` tasks
- [x] 2.2 Verify by running `copier copy templates/ml <dest> --trust` against a destination lacking `.answers/.python_project.yml`: the run exits non-zero with the existing "must be run against a destination already generated by templates/python-project" message, and afterward none of the paths listed in 2.1 exist in the destination

## 3. Reorganize ML overlay test files to mirror source layout

- [x] 3.1 Move `templates/ml/tests/test_estimator.py.jinja` and `templates/ml/tests/test_schema.py.jinja` to `templates/ml/tests/{{ package_name }}/`, and `templates/ml/tests/test_train_model.py.jinja` to `templates/ml/tests/{{ package_name }}/tasks/`, and `templates/ml/tests/test_app.py.jinja` to `templates/ml/tests/webapp/`, fixing any relative imports/fixture paths broken by the move (none of the four files used relative imports, so no import paths needed changing for the move itself; a pre-existing, unrelated ruff import-sort ordering issue in `test_app.py.jinja` was fixed while verifying)
- [x] 3.2 Verify by overlaying `templates/ml` onto a fresh `templates/python-project`-generated destination and confirming `tests/<package_name>/test_estimator.py`, `tests/<package_name>/test_schema.py`, `tests/<package_name>/tasks/test_train_model.py`, and `tests/webapp/test_app.py` all exist, with no `test_estimator.py`, `test_schema.py`, `test_app.py`, or `test_train_model.py` directly under `tests/`
- [x] 3.3 Run `uv run poe check` in the generated destination and confirm all moved tests are collected and pass

## 4. Update regression suite and docs

- [x] 4.1 Update `tests/bdd/ml_overlay_test/ml_overlay_test.feature` (and step bindings if needed) to assert the new test file paths from Task 3 instead of the old flat `tests/test_*.py` paths (the existing happy-path scenario now also asserts the old flat paths do NOT exist; no step-binding changes were needed - the generic `the following files exist`/`do not exist` steps already covered this)
- [x] 4.2 Add a scenario to `tests/bdd/ml_overlay_test/ml_overlay_test.feature` covering the empty-`package_name` failure from Task 1 (destination `.`, expect non-zero exit and no `src/` files created)
- [x] 4.3 Add a scenario to `tests/bdd/ml_overlay_test/ml_overlay_test.feature` covering the cleanup-on-failed-prerequisite behavior from Task 2 (destination missing `.answers/.python_project.yml`, expect non-zero exit and none of the listed paths present afterward) - implemented by extending the existing "fails clearly" scenario for this same condition with cleanup assertions, rather than a fully separate scenario, to avoid re-running an identical setup twice
- [x] 4.4 Search `templates/ml/docs/ML_README.md.jinja` for any test-path references and update them to match the new layout (none found; no changes needed)
- [x] 4.5 Run the full `tests/bdd/ml_overlay_test` suite (`uv run pytest tests/bdd/ml_overlay_test`) and confirm all scenarios pass (3 passed)
