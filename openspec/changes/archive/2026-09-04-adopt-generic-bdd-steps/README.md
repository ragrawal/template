# adopt-generic-bdd-steps

Rewrite the regression suite's Gherkin scenarios to show literal, runnable CLI commands, executed by small generic/reusable step definitions instead of one bespoke function per scenario
